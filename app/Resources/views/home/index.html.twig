{% extends 'base.html.twig' %}

{% block content %}
    <p>
        Articles
    </p>

    <ul>
        {% for article in articles %}
            <li>
                {{ article.title }}
                <br />
                {{ article.subtitle }}
            </li>
        {% endfor %}
    </ul>

    <p>
        Live links
    </p>

    <ul>
        {% for live_link in live_links %}
            <li>
                <a href="{{ live_link.link }}">
                    {{ live_link.title }}
                </a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
